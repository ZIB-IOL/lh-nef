[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    gpu: tests that require CUDA (auto-skipped if no GPU is visible)
    regression: numerical regression tests against frozen paper checkpoints
